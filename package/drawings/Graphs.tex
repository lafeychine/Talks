\usepackage{memorygraphs}
\usepackage{drawstack}
\usepackage{bytefield}
\usetikzlibrary{decorations.pathreplacing}

% Deprecated code

\tikzset{
    every edge/.style={draw, thick},
}

\tikzset{
    Node/.style args={#1,#2}{#1, circle, fill=#2, draw, thick},
    WhiteNode/.style={Node={fg, white}},
    GrayNode/.style={Node={fg, gray}},
    BlackNode/.style={Node={bg, black}},
}

\tikzset{
    EdgeNode/.style={rectangle, fill=bg, draw, scale=0.8},
    EdgeNode/.style={rectangle, fill=bg, draw, scale=0.8},
}

\tikzset{
    Surligned/.append style={draw=red, thick},
}
